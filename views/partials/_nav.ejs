<nav class="home-page-navbar navbar navbar-default navbar-fixed-">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <h1 id="BEAGames">BEA Games</h1>
      <!-- <img class="logo" src="/images/logo.jpg"> -->
    </div>

   <!--  check user logged in -->

<% if (!userID) { %>

    <div class="collapse navbar-collapse" >
      <h5 id="login">Please log in</h2>
        <form method="POST" action="/login">
            Email: <input type="email" name="email">
            Password: <input type="password" name="password">
            <button type="login" class="btn btn-login btn-lg">Login</button>
        </form>
    </div>
    <% } else { %>

      <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
       <form method="POST" action="/logout" class="navbar-form navbar-right">
      <button class="btn btn-default">Logout</button>
      <div id="login-state">Your email:<%= name %></div>
      </form>
    </div><!-- /.navbar-collapse -->
       <% } %>
  </div><!-- /.container-fluid -->
</nav>



